# CooperAI deck — 15 image slides

Open `index.html` in a browser. Use arrow keys or on-screen controls; Full screen starts presentation mode. All 15 numbered PNGs also work independently. Images are approximately 1672 × 941 pixels.

Built with the built-in image-generation tool. Slide 2 supplies the common title/layout reference. The cover is an AI-generated illustrative office scene. Generation prompts are in `prompts.md`; cover refinements are in `consistency-prompts.md`.

Sources: current README.md, SKILL.md, CALL_BRIEF.md, account_profile.md, and SKILL_WORKFLOW_VALIDATION.md. Customer answers and savings are assumptions/targets. Test counts come from recorded validation; no product tests or live demo were rerun solely to create the deck. The fictional correction example matches the native-Q&A fixture.

Required inputs are exactly one CSV and one insurance PDF. Request JSON is not part of the current workflow. The current product supports native questions with chat fallback and batch corrections. No current skill ZIP or production deployment is claimed. The presentation ZIP contains slides, not the runtime skill.

All slides were visually inspected for content and readability. Raster-generated typography and placement remain approximate.
